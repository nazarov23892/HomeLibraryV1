@page "/"
@using HomeLibrary.AL.Services

@model BooksModel

@{
    Layout = "_Layout";
}

<h1 class="mb-4"> Список книг</h1>

@if (!Model.Books.Any())
{
    <div class="alert alert-info">Книг пока нет.</div>
}
else
{
    <table class="table table-sm table-bordered table-striped">
        <thead>
            <tr>
                <th>#</th>
                <th>Название</th>
                <th>Автор</th>
                <th>Год</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var book in Model.Books)
            {
                <tr>
                    <td>@book.Id</td>
                    <td>@book.Title</td>
                    <td>@book.Author</td>
                    <td>@book.PublishYear</td>
                </tr>
            }
        </tbody>
    </table>
}

@functions {

    public class BooksModel : PageModel
    {
        readonly IBookService _bookService;
        public IEnumerable<BookListDto> Books { get; private set; } = [];

        public BooksModel(IBookService bookService)
        {
            _bookService = bookService;
        }

        public async Task OnGet(CancellationToken cancellationToken)
        {
            Books = await _bookService.GetListsAsync(cancellationToken);
        }
    }
}
